/**
 * config.example.js
 * ---------------------------------------------------------------------
 * Template for config.js, which is NOT checked into git because it
 * holds your Supabase project's credentials.
 *
 * To enable the dashboard:
 *   1. Copy this file to config.js (same folder).
 *   2. Fill in your Supabase project's URL and anon key below
 *      (Supabase → Project Settings → API).
 * ---------------------------------------------------------------------
 */

const SUPABASE_URL = 'https://your-project.supabase.co';
const SUPABASE_ANON_KEY = 'your-anon-key';
